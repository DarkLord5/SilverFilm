{% extends 'base.html.twig' %}

{% block title %}Child{% endblock %}

{% block body %}
    <h1>Child</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ child.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ child.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ child.password }}</td>
            </tr>
            <tr>
                <th>Nickname</th>
                <td>{{ child.nickname }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ child.name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ child.surname }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ child.roles ? child.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('child_index') }}">back to list</a>

    <a href="{{ path('child_edit', {'id': child.id}) }}">edit</a>

    {{ include('child/_delete_form.html.twig') }}
{% endblock %}
